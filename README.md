# `@dicether/eslint-config`

> Dicether [Eslint](https://eslint.org) config.

## Usage

**Install**:

```bash
$ npm install --dev @dicether/eslint-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
    "extends": [
        "@dicether/eslint-config",
    ]
}
```

